# Optional SonarCloud configuration.
# The pipeline runs Semgrep by default so it works with no tokens.
# To enable SonarCloud, add SONAR_TOKEN as a repository secret and
# uncomment the Sonar job in .github/workflows/devsecops.yml.

sonar.projectKey=nagarajchalla_devsecops-pipeline
sonar.organization=nagarajchalla
sonar.sources=app
sonar.tests=app/tests
sonar.python.version=3.12
sonar.exclusions=**/tests/**,**/*.tf
